name: learn-github-actions
on: [push]
jobs:
    check-bats-version:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: docker://mcr.microsoft.com/playwright:bionic
            - run: yarn
            - run: yarn ts-node index.ts

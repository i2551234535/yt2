name: learn-github-actions
on: [push]
jobs:
  check-bats-version:
    runs-on: mcr.microsoft.com/playwright:bionic
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm install -g bats
      - run: bats -v

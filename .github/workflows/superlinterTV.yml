name: Super-Linter

# Run this workflow every time a new commit pushed to your repository
on: push

jobs:
    # Set the job key. The key is displayed as the job name
    # when a job name is not provided

    super-lint:
        # Name the Job
        name: Lint code base
        # Set the type of machine to run on
        runs-on: ubuntu-latest
        container:
            image: docker://mcr.microsoft.com/playwright:bionic

        steps:
            # Checks out a copy of your repository on the ubuntu-latest machine
            - name: Checkout code
              uses: actions/checkout@v2

            # Runs the Super-Linter action
            - name: Setup Super-Linter
              run: yarn

            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts

    super-lint-2:
        # Name the Job
        name: Lint code base
        # Set the type of machine to run on
        runs-on: ubuntu-latest
        container:
            image: docker://mcr.microsoft.com/playwright:bionic

        steps:
            # Checks out a copy of your repository on the ubuntu-latest machine
            - name: Checkout code
              uses: actions/checkout@v2

            # Runs the Super-Linter action
            - name: Setup Super-Linter
              run: yarn

            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts

    super-lint-3:
        # Name the Job
        name: Lint code base
        # Set the type of machine to run on
        runs-on: ubuntu-latest
        container:
            image: docker://mcr.microsoft.com/playwright:bionic

        steps:
            # Checks out a copy of your repository on the ubuntu-latest machine
            - name: Checkout code
              uses: actions/checkout@v2

            # Runs the Super-Linter action
            - name: Setup Super-Linter
              run: yarn

            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts

    super-lint-4:
        # Name the Job
        name: Lint code base
        # Set the type of machine to run on
        runs-on: ubuntu-latest
        container:
            image: docker://mcr.microsoft.com/playwright:bionic

        steps:
            # Checks out a copy of your repository on the ubuntu-latest machine
            - name: Checkout code
              uses: actions/checkout@v2

            # Runs the Super-Linter action
            - name: Setup Super-Linter
              run: yarn

            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts

    super-lint-5:
        # Name the Job
        name: Lint code base
        # Set the type of machine to run on
        runs-on: ubuntu-latest
        container:
            image: docker://mcr.microsoft.com/playwright:bionic

        steps:
            # Checks out a copy of your repository on the ubuntu-latest machine
            - name: Checkout code
              uses: actions/checkout@v2

            # Runs the Super-Linter action
            - name: Setup Super-Linter
              run: yarn

            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
            - name: Run Super-Linter
              env:
                  DEFAULT_BRANCH: main
                  MONGO_URI: ${{ secrets.MONGO_URI }}
              run: yarn ts-node indexTV.ts
